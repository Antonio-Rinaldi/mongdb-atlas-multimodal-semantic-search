name: multimodal-semantic-vector-search

services:
  mongodb:
    container_name: mongodb
    image: mongodb/mongodb-atlas-local
    environment:
      - MONGODB_INITDB_DATABASE=multimodal-semantic-vector-search
    ports:
      - "27017:27017"
